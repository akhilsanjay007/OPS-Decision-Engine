# Copy to `.env` in this directory (or set in the process environment).
# Paths may be absolute, or relative to the backend/ folder.

# OPENAI_API_KEY=sk-...

# Runtime asset paths (defaults match repo layout under backend/)
# MODEL_PATH=models/priority_stage5_svm_pipeline.joblib
# CHROMA_DB_DIR=data/chroma
# KB_PATH=data/processed/rag_knowledge_base.jsonl
